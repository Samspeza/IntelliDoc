# URL base da Api. Em desenvolvimento local, o proxy do Vite (vite.config.ts)
# já redireciona /api para http://localhost:8080 - normalmente deixe vazio
# aqui e use apenas o path relativo "/api" (ver shared/lib/api-client.ts).
VITE_API_URL=